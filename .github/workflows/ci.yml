name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  rust:
    name: Rust ${{ matrix.os }}
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
        with:
          components: clippy, rustfmt
      - uses: Swatinem/rust-cache@v2
      - name: cargo fmt
        run: cargo fmt --all -- --check
      - name: cargo clippy
        run: cargo clippy --workspace --all-targets -- -D warnings
      - name: cargo test
        run: cargo test --workspace --release

  python:
    name: Python ${{ matrix.python }} on ${{ matrix.os }}
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]
        python: ["3.9", "3.12"]
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python }}
      - uses: dtolnay/rust-toolchain@stable
      - uses: Swatinem/rust-cache@v2
      - name: build, install, and test
        shell: bash
        run: |
          python -m venv .venv
          if [ -f .venv/Scripts/activate ]; then
            source .venv/Scripts/activate
          else
            source .venv/bin/activate
          fi
          python -m pip install --upgrade pip
          python -m pip install maturin pytest numpy scipy
          maturin develop --release
          pytest -v tests/
      - name: verify wheel runtime dependencies
        shell: bash
        run: |
          if [ -f .venv/Scripts/activate ]; then
            source .venv/Scripts/activate
          else
            source .venv/bin/activate
          fi
          maturin build --release --out dist
          python -m venv .wheel-venv
          if [ -f .wheel-venv/Scripts/activate ]; then
            source .wheel-venv/Scripts/activate
          else
            source .wheel-venv/bin/activate
          fi
          python -m pip install dist/*.whl
          python -c "import mvkit; import mvkit.mfg; import mvkit.poc"
